{
  "name": "@sptk-epb/visual-audit",
  "version": "0.1.0",
  "description": "Shared Playwright visual-audit library (screenshots, layout inspection, overflow detection) for SPTK-EPB projects.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.mjs",
    "./capture": "./src/capture.mjs",
    "./inspect": "./src/inspect.mjs",
    "./overflow": "./src/utils/overflow.mjs",
    "./describe": "./src/utils/describe.mjs"
  },
  "main": "./src/index.mjs",
  "files": [
    "src/"
  ],
  "scripts": {
    "setup": "bash scripts/dev-setup.sh",
    "lint": "echo 'lint: no linter configured yet'",
    "typecheck": "echo 'typecheck: JSDoc only — no tsc step'",
    "test": "node --test src/**/*.test.mjs || true"
  },
  "peerDependencies": {
    "playwright": "^1.45.0"
  },
  "devDependencies": {
    "playwright": "^1.45.0"
  },
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SPTK-EPB/sptk-visual-audit.git"
  },
  "license": "UNLICENSED"
}
